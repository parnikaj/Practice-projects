#include <stdio.h>
int main(){

char name [25]; 
printf("\nWhat is your name?"); 
scanf("%s",&name);
printf("\nYour name is %s",name);  

    int age; 
    printf("\nHow old are you?"); 
    scanf("%d",&age);
    printf("\nYou are %d years old",age); 

    return 0; 
}
